/*
create a storage container for the output
add the two inputs together and the result of that expression
will be assigned to te output storage container
display the output storage container
*/
function solution(param1, param2) {
  var sum = param1 + param2;
  return sum;
}
